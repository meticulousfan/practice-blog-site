### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  message: String
  token: String
  user: User
}

type Blog {
  author: User!
  authorId: Int
  category: String
  comments: [Comment]
  description: String
  id: Int
  title: String
}

type Comment {
  author: User!
  authorId: Int
  blogId: Int
  description: String
  id: Int
}

type Mutation {
  createBlog(category: String!, description: String!, title: String!): Blog!
  createComment(blogId: Int!, description: String!): Comment
  editBlog(category: String!, description: String!, id: Int!, title: String!): Blog!
  removeBlog(id: Int!): Blog!
  signin(email: String!, password: String!): AuthPayload!
  signup(email: String!, name: String!, password: String!): AuthPayload!
}

type Query {
  blog(id: Int!): Blog!
  blogs: [Blog]!
  comments: [Comment]!
  getProfileByToken: User!
  users: [User]!
}

enum Role {
  ADMIN
  USER
}

type User {
  blogs: [Blog]
  comments: [Comment]
  email: String
  id: Int
  name: String
  password: String
  role: Role
}